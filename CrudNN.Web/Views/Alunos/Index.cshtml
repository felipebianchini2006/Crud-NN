@using System.Linq
@model IEnumerable<Aluno>
@{
    ViewData["Title"] = "Alunos";
    var search = ViewData["Search"] as string;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h3 mb-0">Alunos</h1>
    <a class="btn btn-primary" asp-action="Create">Novo aluno</a>
</div>

<form method="get" class="row gy-2 gx-2 align-items-end mb-3">
    <div class="col-sm-5 col-md-4 col-lg-3">
        <label class="form-label" for="search">Buscar por nome, e-mail ou telefone</label>
        <input class="form-control" id="search" name="search" value="@search" />
    </div>
    <div class="col-sm-auto">
        <button type="submit" class="btn btn-outline-secondary">Pesquisar</button>
    </div>
    @if (!string.IsNullOrWhiteSpace(search))
    {
        <div class="col-sm-auto">
            <a class="btn btn-link" asp-action="Index">Limpar</a>
        </div>
    }
</form>

@if (!Model.Any())
{
    <div class="alert alert-info">Nenhum aluno encontrado.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped align-middle">
            <thead>
            <tr>
                <th>Nome</th>
                <th>E-mail</th>
                <th>Telefone</th>
                <th class="text-end">Ações</th>
            </tr>
            </thead>
            <tbody>
            @foreach (var aluno in Model)
            {
                <tr>
                    <td>@aluno.Nome</td>
                    <td>@aluno.Email</td>
                    <td>@aluno.Telefone</td>
                    <td class="text-end">
                        <a class="btn btn-sm btn-outline-secondary me-1" asp-action="Details" asp-route-id="@aluno.Id">Detalhes</a>
                        <a class="btn btn-sm btn-outline-primary me-1" asp-action="Edit" asp-route-id="@aluno.Id">Editar</a>
                        <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@aluno.Id">Excluir</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}
